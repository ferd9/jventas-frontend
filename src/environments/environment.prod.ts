export const environment = {
  production: true,
  // Ruta relativa: asume que en producción el frontend se sirve desde el
  // mismo origen que el backend (proxy reverso) -- ajustar si no es el caso.
  apiUrl: '/api',
};
